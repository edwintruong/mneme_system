# Continuation prompt

Continue the Mneme Flutter implementation from `docs/PROGRESS.md`. Read `AGENTS.md`, `kit/docs/ARCHITECTURE.md`, and `kit/docs/FIGMA_MAP.md` first. Use Figma design context on the exact target node before editing a screen. Preserve existing local SQLite behavior and shared design tokens. Work in a small runnable milestone, run `kit/scripts/check.sh`, update progress, and make a local commit using `feat(scope): ...`.
